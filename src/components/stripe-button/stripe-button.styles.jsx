import styled from 'styled-components';
import StripeCheckout from 'react-stripe-checkout';

export const StripeCheckoutContainer = styled(StripeCheckout)`
    margin-left: auto;
    margin-top: 20px;
    margin-bottom: 30px;
`;